# Build output
/target
/target-alt
/dist
**/dist/
node_modules/
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/schemas/

# Databases (project DBs, SQLite temp files)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.locust.db*

# Logs
*.log
logs/

# Environment and secrets
.env
.env.*
!.env.example
*.key
*.pem

# Tauri signing keys — must never be committed
apps/desktop/src-tauri/*.key
apps/desktop/src-tauri/*.pub
tauri.key
tauri.pub

# OS
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/
.Spotlight-V100
.Trashes

# Editors
.idea/
.vscode/
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~

# Claude Code local state (per-machine, not shared)
.claude/settings.local.json
.claude/cache/
memory/

# Project-specific caches / backups
backups/
.locust-cache/
data/

# Session / scratch notes
session/
notes.md
TODO.local.md

# Generated skill registry cache (gentle-ai)
.atl/

# Built KiriKiri proxy DLLs and the renamed original system DLL they forward to.
# Build from docs/vn-tools/proxy/src instead of committing binaries.
docs/vn-tools/proxy/*.dll
